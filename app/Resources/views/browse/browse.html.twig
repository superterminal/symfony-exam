{% extends 'base.html.twig' %}

{% block body %}

    <div class="container body-content">
        <h1>Browse movies here</h1>
        <div class="row">
{#            {% for movie in movies %}#}
{#                <div class="col-md-4">#}
{#                    <article>#}

{#                        <a href="{{ path('view_movie', {id: movie.id}) }}">#}
{#                            <img src="{{ loadImage(movie.posterPath) }}" alt="" width="200px" height="300px"><br>#}
{#                            <h2>{{ movie.originalTitle }}</h2>#}
{#                            <h4>{{ movie.id }}</h4>#}
{#                        </a>#}

{#                    </article>#}
{#                </div>#}


{#            {% endfor %}#}
{#            <div class="navigation text-center">#}
{#                {{ knp_pagination_render(movies) }}#}
{#            </div>#}
        </div>

        <form action="{{ path('browse_action') }}" method="post">
            <div class="form-group">
                <label class="col-sm-1 control-label" for="search">Search for movie</label>
                <div class="col-sm-3">
                    <input type="search" class="form-control" id="search" placeholder="Search" name="browse[input]">
                </div>
                <label class="col-sm-1 control-label" for="order">Order by</label>
                <select class="form-control" id="order" name="browse[orderBy]">
                    <option value="popularity.desc">Popularity desc</option>
                    <option value="popularity.asc">Popularity asc</option>
                    <option value="release_date.asc">Release date asc</option>
                    <option value="release_date.desc">Release date desc</option>
                    <option value="average_vote.asc">Average vote asc</option>
                    <option value="average_vote.desc">Average vote desc</option>
                </select>
                <label class="col-sm-1 control-label" for="order">Genres</label>
                <select class="form-control" id="order" name="browse[genre]">
                    <option disabled selected></option>
                    {% for genre in genres %}
                    <option value="{{ genre.id }}">{{ genre.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Search</button>
        </form>
    </div>
{% endblock %}

